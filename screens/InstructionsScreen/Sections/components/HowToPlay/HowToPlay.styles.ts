import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	bodyText: {
		color: "#CBD5E1",
		fontSize: 14,
		lineHeight: 22,
	},

	labelText: {
		color: "#94A3B8",
		fontSize: 13,
		fontWeight: "600",
		letterSpacing: 0.2,
	},
});

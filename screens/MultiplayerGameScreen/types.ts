// Retângulo de layout de um chip (x e largura relativos à trilha de jogadores),
// usado para posicionar o "blob" deslizante sob o jogador ativo.
export type ChipRect = { x: number; width: number };
